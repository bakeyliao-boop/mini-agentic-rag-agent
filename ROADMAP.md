# mini-agentic-rag-agent Roadmap

更新时间：2026-08-20

本文档描述项目当前能力、近期开发主线和后续演进边界。它不是单次需求清单，也不以堆叠工具数量为目标；每项能力都需要通过固定语料、真实运行结果和质量—成本指标验收。

## 1. 项目定位

本项目用于验证普通 RAG 与 Agentic RAG 在知识读取方式上的关键差异：

```text
普通 RAG
Query → 单次 Search → Top-K Chunks → Answer

Agentic RAG
Query → 定位资源 → Search → Read 原文 → 继续补证 → Answer + Citations
```

当前核心问题不是“Agent 能否调用工具”，而是：

> 面对证据分散在长文档、多个 Chunk 或多个文件中的问题，Agent 能否比固定单次 Top-K 更完整地找到证据，同时把额外工具调用、Token 和时延控制在可接受范围内。

项目当前是本地可复现的研究与学习原型，不是生产级知识服务平台。

## 2. 核心边界

### 当前重点

- 虚拟路径和统一 Markdown 读取接口。
- `ls / glob / search / read` 四工具协作。
- 长文档、多 Chunk 和多文件证据补全。
- 原文证据注册、引用与服务端复核。
- 传统 RAG、Agentic RAG 和诊断组的公平对照。
- 工具调用数、Token、时延和稳定性评测。

### 当前不作为主线

- Redis、分布式缓存和高 QPS 架构。
- 多租户权限、组织隔离和复杂 ACL。
- PostgreSQL 物化路径索引。
- 完整多模态自动训练平台。
- Skill 平台化和长期记忆。
- 自动把所有问题路由到 Agent。

这些方向只有在当前 Pilot 证明深度检索存在稳定收益后才进入实施阶段。

## 3. 状态定义

| 状态 | 含义 |
|---|---|
| 已完成 | 代码、自动测试和对应真实验证均已完成 |
| 已实施待验证 | 代码和测试已落地，但尚缺真实语料或完整实验验收 |
| 进行中 | 已有部分实现或实验结果，但验收尚未闭环 |
| 部分完成 | 核心能力可用，但仍有明确未完成的子能力 |
| 待做 | 方向和前置条件明确，尚未实现 |
| 探索 | 需要先用评测或原型确认必要性 |
| 后置 | 有潜在价值，但当前不排期 |

## 4. 路线总表

路线总表按当前关注优先级排序：近期进行中、待做和探索项在前；已经完成但仍需观察的能力居中；当前不排期的能力集中在最后。

| 能力方向 | 状态 | 已完成 | 待完成 |
|---|---|---|---|
| 长文档 Pilot B 组 | 进行中 | 临时诊断实验已观察到 12/12 的多轮补证能力 | 固定正式预算与统一规则，完成 3 次可复现运行；不能把临时 `tool10` 直接升级为正式标准 |
| 长文档 Pilot C 组 | 待做 | 已在规范中固定三个业务子查询 | 运行 3 次固定子查询 Top-5，区分“额外检索预算”与“Agent 自主规划”收益 |
| Pilot 自动评分与报告 | 部分完成 | A 组检索、回答和 12 答案点语义评分已完成 | 统一汇总 A/B/C 的覆盖、证据、调用、Token、时延和稳定性 |
| 检索与读取效率优化 | 探索 | 已确认 Agent 能跨 Chunk 补证；临时成功运行约 13万～15万 Token | 基于 B/C 轨迹识别主要成本来源，再选择一个变量优化，不提前堆叠中间件 |
| 长文档相关性阈值 | 探索 | 小语料已实现 `relevant/uncertain/none` 三态信号 | 验证 0.6/0.35 阈值是否适用于 86 Chunk 长文档；未校准前不得作为 Pilot 硬门槛 |
| 可观测性 | 部分完成 | 已记录工具轨迹、Token、引用和总体时延 | 增加分阶段耗时、检索候选摘要和明确终止原因，支持解释工具成本 |
| Sparse / Hybrid / Rerank | 探索 | 当前 Dense 检索链路稳定可用 | 只有在 Pilot 证明召回方式是主要瓶颈时，再依次评估 Sparse、Hybrid 和 Rerank |
| 统一资源投影 | 部分完成 | 文档转换脚本、语义路径映射和统一 Markdown 读取已存在 | 先完成文档类验证；图片、音视频和表格解析暂不进入近期主线 |
| 长文档 Pilot A 组 | 已完成 | 单文档 4203 行、86 Chunks；单次 Top-5；答案覆盖 3/12（25%） | 作为固定传统 RAG 基线保留，不再针对结果反向调整题目 |
| 虚拟知识文件系统 | 已完成 | 虚拟路径规范化、安全映射、内存路径快照、稳定 `ls/glob/read` | 仅在真实规模暴露性能问题时评估 SQLite 或数据库物化路径索引 |
| Dense 向量检索 | 已完成 | Chroma、DashScope Embedding、路径范围过滤、Top-5 候选和三态信号 | 保持为当前基线；增强项由 Pilot 结果触发 |
| Agent 知识工具链 | 已完成 | LangChain Agent、四工具、Prompt-V1.6、固定工具上限、内存会话 | 不再针对单条样本增加 Prompt 防御规则 |
| 原文证据与引用 | 已完成 | EvidenceRegistry、GroundedAnswer、path/line/quote 校验、无证据降级 | 用长文档继续验证跨段与跨页引用完整性 |
| 检索停止与成本治理 | 已完成 | 无证据时确定性停止；固定 10 题总 Token 降低 26.2% | 长文档中重新测量停止信号，不直接沿用小语料结论 |
| FastAPI 双入口 | 已完成 | `/chat/traditional` 与 `/chat/agentic`、共享运行时、统一错误结构 | Pilot 定稿后再讨论显式模式选择或自动路由 |
| 固定 10 题小语料评测 | 已完成 | 答案覆盖 100%、引用 17/17 有效、工具流程评分、语义 Judge 和 A/B 报告 | 继续作为回归集，不作为长文档能力证明 |
| 高 QPS 与低延迟 | 后置 | 尚无并发与容量基线 | Pilot 证明稳定质量收益后，再评估缓存、并发、限流和运行时复用 |
| 持久化会话与长期记忆 | 后置 | 当前使用进程内 checkpointer | 先区分短期会话恢复和长期记忆，再决定是否引入 Redis 或数据库 |

## 5. 当前最高优先级：长文档 A/B/C Pilot

### 5.1 已固定实验

- 题目 ID：`multi-chunk-guide-001`
- 语料：一份长文档，4203 行，86 个 Chunks。
- 答案点：3 项业务 × 4 个字段，共 12 点。
- A 组：完整问题单次 Top-5。
- B 组：Agent 自主 `glob/search/read`。
- C 组：三个固定子查询分别 Top-5，合并去重后回答。

### 5.2 当前真实结果

| 组别 | 状态 | 答案点覆盖 | 调用与成本 | 结论 |
|---|---|---:|---|---|
| A：单次 Top-5 | 正式完成 | 3/12（25%） | 1 次检索；回答约 2500 Token；约 9.36 秒 | 固定单次检索未覆盖远距离章节 |
| B：Agent，6 次预算 | 临时实验 | 0/12 | 最终降级为 insufficient | 当前正式预算不足以稳定完成任务 |
| B：Agent，10 次诊断预算 | 非正式诊断 | 两次 12/12，一次失败 | 9～11 次调用；约 13万～16万 Token | 已出现跨 Chunk 完整补证能力，但成本和稳定性不可接受 |
| C：三子查询 | 未运行 | — | — | 用于判断收益是否主要来自查询拆分和额外召回预算 |

### 5.3 下一步执行顺序

1. 固定 B 组正式运行协议，不直接把临时 `tool10` 当成新标准。
2. 使用相同模型、Embedding、语料快照和回答约束运行 B 组三次。
3. 实现并运行 C 组三次。
4. 生成 A/B/C 统一对比报告。
5. 根据真实轨迹选择一个主要成本瓶颈进行单变量优化。

禁止在 C 组完成前宣称 Agent 的优势来自自主规划，因为当前优势也可能主要来自更多检索次数和更大的证据预算。

## 6. Pilot 验收指标

### 质量指标

- `answer_point_coverage`
- `grounded_answer_point_coverage`
- `retrieved_gold_region_coverage`
- Citation 原文有效率
- 三项业务是否全部有原文支撑

### 成本指标

- 工具或检索调用次数
- 返回 Chunk 数量
- 输入 Token、输出 Token、总 Token
- 端到端时延
- 单次 search、read 和模型调用耗时

### 稳定性指标

- 每组至少运行 3 次。
- 分别报告成功率和覆盖率中位数，不只展示最好的一次。
- 工具报错、超限降级和无结构化回答必须计为失败，不得从统计中删除。

### 区分度规则

当前 Pilot 规范采用：

- Agent 覆盖率中位数至少 85%。
- Agent 相比 A 组至少提升 20 个百分点。
- Agent 必须对三项业务都提供可信证据。

同时必须报告成本。高覆盖但 Token、时延或失败率不可接受时，只能认定“能力存在”，不能认定“方案可用”。

## 7. Pilot 完成后的候选阶段

### 阶段 11：预算感知的深度检索

目标：让 Agent 在有限预算内判断“已覆盖哪些答案点、还缺哪些证据”，减少重复 search 和无目标 read。

候选方向：

- 将复杂问题拆成可检查的子任务。
- 维护当前证据覆盖状态。
- search 只定位，read 按缺失证据定向展开。
- 达到完成条件后确定性停止。
- 将工具预算作为运行状态，而不是只在末尾报错。

是否实施由 A/B/C 结果决定，不提前写死具体中间件。

### 阶段 12：检索链路增强

只有在阶段 11 仍暴露召回瓶颈时，依次评估：

1. 标题与章节信息增强切块。
2. candidate count 与最终上下文数量分离。
3. Sparse/BM25 精确词召回。
4. Dense + Sparse Hybrid。
5. Rerank 和每资源最大 Chunk 数。

每次只引入一个变量，并与 A/B/C 基线比较。

### 阶段 13：统一资源视图扩展

在 Markdown 文档链路稳定后，再逐类验证：

- PDF/Office 的页码或幻灯片定位。
- 图片 OCR/VLM 文字与原图定位。
- 音视频 ASR、时间段和关键帧定位。
- 表格的 sheet/table 定位。

统一接口保持 `path + read + locator`，但不抹掉资源类型差异。

### 阶段 14：服务化与性能

只有在存在真实并发需求后再进入：

- 路径快照持久化或数据库物化索引。
- 多实例共享运行时和索引版本。
- 查询缓存、响应缓存和负缓存。
- QPS 限流、超时、取消和降级。
- 会话 checkpoint 持久化。
- 权限、审计和敏感信息隔离。

## 8. 历史数据与实验兼容策略

- 固定 10 题 `education-v1` 继续作为小语料回归集，不因长文档 Pilot 改写历史问题或结果。
- 长文档 Pilot 使用独立语料规范、独立 Chroma 目录和独立结果文件，不覆盖原有 `data/chroma`。
- Embedding 模型、维度、批次、Chat 模型、Prompt 和语料快照必须写入实验配置；任一项变化都需要生成新结果，不覆盖旧结果。
- 原始 Markdown 变更后必须重建 Pilot 索引，并更新语料版本；不能继续沿用旧的 86 Chunk 结论。
- 调试运行、失败运行和临时工具预算实验默认只保存在本地；只有命名、运行次数和评分完整的正式结果才进入版本库。
- 不默认重建所有历史索引；只有对应语料或切块规则发生变化时才按版本重建。

## 9. 决策原则

1. 先用评测证明问题存在，再增加复杂度。
2. 不以单次成功轨迹替代稳定性结论。
3. 不把更多检索预算产生的收益误写成 Agent 规划收益。
4. 不只比较正确率，必须同时比较 Token、时延和失败率。
5. Search 候选不是最终证据，知识回答必须经过 read 和原文复核。
6. 保留快速 RAG 与深度 Agent 两种路径，不强制所有问题走 Agent。
7. 原型阶段优先保持本地、可读、可测试，不提前复制生产平台复杂度。

## 10. 下一里程碑完成定义

长文档 Pilot 完成需要同时满足：

- [x] Pilot 规范、语料和 12 个答案点固定。
- [x] 独立 Chroma 索引可复现，Chunk 数固定为 86。
- [x] A 组检索、回答和评分完成。
- [ ] B 组三次正式运行完成。
- [ ] C 组三次正式运行完成。
- [ ] A/B/C 使用同一模型和语料快照。
- [ ] 质量、工具调用、Token、时延和稳定性统一汇总。
- [ ] 明确区分“额外检索预算收益”和“Agent 自主规划收益”。
- [ ] 形成正式结论并据此选择下一阶段，不因单个失败样本继续堆叠规则。
