# Knowledge Agent Prompt 版本记录

每次修改 Agent 决策规则时新增版本，不直接覆盖历史 Prompt。运行中的版本由
`app/prompts.py` 内的 `KNOWLEDGE_AGENT_PROMPT_VERSION` 指定。

## Prompt-V1.0

- 建立 `ls/search/read` 三工具职责。
- 要求知识回答在最终回答前使用 `read`。

## Prompt-V1.1

- 增加目录范围规则。
- `ls` 找到具体目录后，要求将该目录作为 `search.path`。

## Prompt-V1.2

### 调整内容

- `read` 已获得充分证据后立即停止搜索。
- 最终知识回答必须提交 `GroundedAnswer` 和 `evidence_id`。
- 禁止相似关键词重复搜索；证据不足时及时输出 `insufficient`。

### 真实结果

- 普通知识库内问题恢复为 `search -> read -> GroundedAnswer`。
- 知识库外问题可以降级为 `insufficient`，但仍可能耗尽工具调用次数。
- 完整结果见 [Prompt-V1.2 原始结果](evaluation/results/agentic-baseline-qwen3.6-flash-thinking-off-prompt-v1.2.json)。

### 已发现问题

- `directory-001`：Agent 在 `ls("/课程资源")` 已返回“初中、小学”后，仍提交
  `insufficient`。V1.2 没有明确规定纯目录题可以直接依据 `ls` 结果提交
  `answer_type=directory`。
- `ambiguity-001`：问题中的目录名只是知识题范围，但 Agent 将它误判成目录遍历任务；
  先猜测 `/自动控制系统`，失败后从根目录选择“初中”分支逐层 `ls`，最终触发工具调用上限。
- “处理目录或范围问题时逐层 `ls`”的规则覆盖面过大，没有区分纯目录清单与目录范围内的
  文件内容、属性问题。

## Prompt-V1.3

### 实验假设

- 明确区分纯目录题与“目录只是范围”的知识题，可以修复 `directory-001` 和
  `ambiguity-001`，无需先提高工具调用上限。
- 在暂不增加 `glob` 工具的第一轮实验中，可用根路径 `search` 定位未知完整路径，再用
  `read` 取证。

### 调整内容

- 纯目录题仅指直接子项、文件/目录清单和路径存在性问题；`ls` 返回所需子项后立即提交
  `answer_type=directory`，不再调用 `search/read`。
- 目录名只用于限定范围，而用户询问文件内容、属性或资源类型时，仍按知识题处理。
- 完整虚拟路径未知时禁止猜路径和从根目录选择单一分支逐层试探；先从 `/` 使用
  `search`，并只使用工具返回的完整路径。
- 工具报告路径不存在后必须重新发现路径，不能继续拼接错误路径。
- 保留 V1.2 的充分证据停止规则和重复搜索限制。

### 状态

- 已运行新基线验证。
- 原始结果见 [Prompt-V1.3 原始结果](evaluation/results/agentic-baseline-qwen3.6-flash-thinking-off-prompt-v1.3.json)。
- 评分结果见 [Prompt-V1.3 评分](evaluation/results/agentic-baseline-qwen3.6-flash-thinking-off-prompt-v1.3-score.json)。

### 真实结果

| 指标 | Prompt-V1.2 | Prompt-V1.3 | 变化 |
|---|---:|---:|---:|
| 回答类型准确率 | 80% | 100% | +20 个百分点 |
| 答案点覆盖率 | 48% | 64% | +16 个百分点 |
| 目录题准确率 | 0% | 100% | 已修复 |
| 引用覆盖率 | 87.5% | 100% | +12.5 个百分点 |
| `read` 合规率 | 87.5% | 100% | +12.5 个百分点 |
| 工具调用总数 | 32 | 29 | -3 |
| 平均延迟 | 3.94 秒 | 4.66 秒 | +18% |
| 总 Token | 85,755 | 105,888 | +23.5% |

目标题验证：

- `directory-001` 从 `insufficient` 修复为 `directory`，正确回答直接子目录是“初中、小学”。
  但首次调用仍猜测了不存在的 `/课程资源目录`，工具调用次数维持 3 次，说明“禁止猜路径”
  没有完全生效。
- `ambiguity-001` 从 `insufficient` 修复为 `knowledge`，执行轨迹从错误分支的 7 次 `ls`
  改为一次失败 `ls`、一次根路径 `search` 和两次 `read`，工具调用从 7 次降至 4 次；两个资源类型
  和两条引用都正确。但首次调用仍猜测了 `/自动控制系统`。

### 遗留问题

- V1.3 Prompt 更长，使多数普通知识题即使工具次数不变，也增加约 600–1000 Token。
- `out-of-scope-001` 连续进行了 6 次相似 `search`，第 7 次触发工具限制。虽然最终正确拒答，
  Token 从 20,489 增至 34,867，延迟从 4.4 秒增至 11.3 秒，是本轮成本回退的主要来源。
- V1.2 已存在“禁止相似关键词重复搜索”的文字规则，但 V1.3 真实运行仍未稳定遵守；后续应评估
  使用代码限制 `search` 次数，而不是继续只增加 Prompt 文字。

### 实验结论

- “区分纯目录题与目录范围内的知识题”假设成立，修复了两道目标题并提升了整体正确性。
- “禁止猜测路径”和“禁止重复搜索”仅靠 Prompt 不能稳定生效。
- V1.3 暂时作为正确性更高的基线保留；下一轮优先治理重复搜索和输入 Token，而不是提高全局工具上限。

## Prompt-V1.4

### 当前问题

- V1.3 虽然修复了 `directory-001` 和 `ambiguity-001` 的回答类型，但两题仍出现了路径猜测。
- 仅在 Prompt 中禁止猜测路径不能稳定生效；`ls` 又只能查看下一层，逐层探索容易消耗工具次数。
- Agent 缺少一个按目录名或文件名直接发现完整虚拟路径的确定性工具。

### 实验假设

- 增加 `glob` 路径发现工具后，Agent 可以先按路径形状定位文件，再使用返回的真实路径调用 `search/read`。
- 将“路径发现”和“内容检索”分开，可以减少错误 `ls`、路径拼接和单分支试探。

### 调整内容

- 工具集合由 `ls/search/read` 调整为 `ls/glob/search/read`。
- 完整虚拟路径未知但已知目录名或文件名时，要求先使用 `glob`。
- `glob` 结果只负责定位，不能作为知识回答证据；知识回答仍必须经过 `read`。
- 后续 `search/read` 只能使用 `glob` 返回的完整虚拟路径，禁止自行猜测或拼接路径。
- 只提供内容关键词、没有目录名或文件名时，仍使用根路径 `/` 的 `search`。

### 状态

- Prompt-V1.4 已写入并启用。
- `glob` 底层函数和工具层测试已经完成。
- 已使用“自动控制系统目录中的两个资源分别是什么类型”进行真实冒烟测试。
- 冒烟回答类型、答案和两条引用均正确，但工具轨迹为：一次错误 `ls`、一次空结果 `glob`、
  一次根路径 `search` 和两次 `read`。
- Agent 为 `glob` 生成了 `pattern="**/*自动控制系统*"`，把目录名误当成文件名模式；
  正确的目录模式应为 `**/自动控制系统/*.md`。
- 本次共调用 5 次工具，消耗 13,058 Token。结论是功能结果通过，但路径决策和调用成本未通过。
- 尚未运行 Prompt-V1.4 完整基线。

## Prompt-V1.5

### 当前问题

- V1.4 要求 Agent 使用 `glob`，但仍由 Agent 自行生成原始 pattern，容易混淆目录段和文件名段。
- 原始 pattern 语法合法时，工具无法判断它是否符合用户的目录或文件名意图。
- 继续增加 glob 语法说明仍依赖模型稳定遵守，不能从接口层消除这类参数错误。

### 实验假设

- 使用结构化 glob 参数，让 Agent 只判断目标是目录还是文件，可以避免模型直接拼接 `*` 和 `**`。
- 底层继续保留通用 pattern 匹配能力，由工具层根据 `target_type` 统一生成 pattern。

### 调整内容

- Agent 调用参数从 `path + pattern` 改为 `path + target + target_type`。
- `target_type=directory` 时生成 `**/目标目录/*.md`。
- `target_type=filename` 时生成 `**/*文件名关键词*.md`；已包含 `.md` 时不重复追加后缀。
- `target` 拒绝空白、路径分隔符、glob 通配符以及 `.`、`..`。
- Prompt 明确禁止传入 pattern，也禁止先把自然语言目录名拼成 `ls` 路径。

### 状态

- Prompt-V1.5 已写入并启用。
- 结构化 glob Schema、目录匹配、文件名匹配和非法 target 测试已经通过。
- 尚未运行 Prompt-V1.5 真实基线。

### 真实冒烟结果

#### 目录目标

问题：`“自动控制系统”目录中的《课程介绍》和《智慧农场》分别是什么类型的资源？`

- Agent 首次调用 `glob(target="自动控制系统", target_type="directory", path="/")`。
- 随后直接 `read` 两个目标文件，没有错误 `ls`，也没有多余 `search`。
- `answer_type=knowledge`，两个资源类型及两条第 6 行引用均正确。
- 工具调用 3 次；输入 11,419 Token，输出 325 Token，总计 11,744 Token。
- 相较 V1.4 同题，工具调用从 5 次降至 3 次；总 Token 从 13,058 降至 11,744，
  减少 1,314（约 10.1%）。

#### 文件名目标

问题：`请查找《智慧农场》这个文件，并说明它是什么类型的资源？`

- Agent 首次调用 `glob(target="智慧农场", target_type="filename", path="/")`。
- 随后直接 `read` 目标文件，没有调用 `ls` 或 `search`。
- 正确回答资源类型为 `video_transcript`，并引用文件第 6 行。
- 工具调用 2 次；输入 6,728 Token，输出 252 Token，总计 6,980 Token。
- 回答中还包含年级、教材、单元和内容介绍，但唯一引用只证明资源类型；该问题已单独记入
  `PENDING.md`，不计为 glob 改造问题。

### 实验结论

- 两次冒烟分别覆盖了明确的目录目标和文件名目标，结构化 glob 参数均生成正确。
- 这两个样例中已消除 V1.4 的原始 pattern 错误、猜测路径和补救性搜索。
- 当前只证明两个明确表达的冒烟样例通过；模糊表达和 Prompt-V1.5 完整基线仍待验证。

## Prompt-V1.6

### 当前问题

- Prompt-V1.5 在判断题型之前先说明了未知目录名应使用 `glob`，而目录题规则只明确排除了
  `search/read`，没有排除 `glob`。
- Prompt-V1.5 的 `directory-001` 虽然最终正确回答“初中、小学”，工具轨迹仍为
  `glob -> search -> ls`；其中前两次调用不能证明目标目录的完整直接子项。
- 离线工具流程标准已确认：纯目录题应只使用 `ls`；可以直接调用目标 `ls`，也可以先从已知祖先
  目录使用一次 `ls` 发现目标路径，最多两次调用。

### 实验假设

- 将题型判断提前到所有 glob 参数规则之前，可以先区分纯目录题和知识题。
- 明确规定“纯目录题只能使用 ls，禁止调用 glob、search 或 read”，并把 glob 规则限定在知识题中，
  可以消除 Prompt-V1.5 的工具路由冲突。
- 保持知识题的结构化 glob、search 定位和 read 取证规则不变，可以把本轮实验控制为单一变量。

### 调整内容

- 在 Prompt 开头先判断目录题与知识题，再进入各自工具规则。
- 纯目录题只能调用 `ls`；完整路径未知时，从最近的已知祖先目录使用 `ls` 发现真实子目录，
  没有更近祖先时从根路径 `/` 开始。
- `glob(target, target_type, path)` 的结构化定位规则只适用于知识题。
- 保留 Prompt-V1.5 的知识题路径发现、候选检索、`read` 证据和及时停止规则。

### 状态

- Prompt-V1.6 已写入并启用。
- 已新增纯目录题仅允许 `ls` 的离线 Prompt 契约测试，等待重新运行。
- 已运行 Prompt-V1.6 完整真实基线。
- 下一步只运行 `directory-001` 真实冒烟，验证工具轨迹是否从 `glob -> search -> ls`
  收敛为一次目标 `ls` 或两次 `ls`；确认后再决定是否运行完整 10 题基线。

### 真实冒烟结果

问题：`课程资源目录下有哪些直接子目录？只列出下一层。`

- 回答类型为 `directory`，正确列出“初中、小学”，`citations=[]`。
- 工具类型已从 V1.5 的 `glob -> search -> ls` 收敛为仅使用 `ls`，说明目录题与知识题的工具边界已经生效。
- 实际轨迹为一次错误 `ls('/课程资源目录')`、一次成功 `ls('/')`、一次成功
  `ls('/课程资源')`；共 3 次调用，仍超过标准上限 2 次，并违反“所有调用成功”的要求。
- 首次调用仍把自然语言中的“课程资源目录”直接拼成虚拟路径，说明仅靠 Prompt 的禁止语句尚未完全消除路径猜测。
- 本次总 Token 为 6,847；V1.5 同题为 9,069，单次观测减少 2,222（约 24.5%），但仍不能据此证明稳定性能提升。

### 本轮结论

- Prompt-V1.6 部分通过：答案正确、没有调用 `glob/search/read`，工具类别路由修复。
- Prompt-V1.6 工具流程未通过：存在一次猜测路径错误且调用总数为 3。
- 现有离线测试已经锁定“自然语言目录名不能直接拼进虚拟路径”，但真实模型仍违反该规则；继续增加同义
  Prompt 断言不能证明行为改善。
- 下一步应讨论并为“只能调用根路径或工具已经发现的目录路径”设计确定性运行时护栏测试；暂不运行
  完整 10 题基线，也不立即增加 Prompt-V1.7。

### 完整真实基线结果

- 10 题全部生成结果；答案类型准确率 100%，目录题和超范围拒答题均回答正确。
- 8 道知识题全部提供引用，共 21 条引用；引用路径准确率与来源有效性均为 100%。
- 工具共调用 28 次，其中成功 26 次、错误 2 次，平均每题 2.8 次。
- 平均延迟 3,621.9 ms，P95 为 6,699.2 ms；总 Token 112,731，平均每题 11,273.1。
- 工具流程按后续确认的“允许错误恢复、限制总调用预算”标准为 9/10 通过；`directory-001`
  在 3 次 `ls` 内恢复并完成，因此通过。
- 唯一失败为 `out-of-scope-001`：最终正确拒答，但轨迹包含 4 次 `search`、3 次 `glob`
  和 1 次错误调用，总 Token 30,071。问题已经从目录路由转移为超范围题的停止策略。

## Prompt-V1.7

### 当前问题

- Prompt-V1.6 已要求“证据不足时提交 insufficient”，但没有为“知识库是否包含某项内容”的
  超范围题规定明确的一次检索预算。
- `out-of-scope-001` 首次 `search` 后仍反复改写查询并切换到 `glob`，最终依靠全局工具调用
  限制结束；回答安全，但 Token、延迟和工具错误均不合理。

### 实验假设

- 对此类超范围内容题明确限定一次 `search`，首次检索没有支撑证据时立即提交
  `answer_type=insufficient`，可以阻止重复 search 和无关 glob。
- 保持 Prompt-V1.6 的目录题、glob 定位、read 取证规则完全不变，可以把本轮实验控制为停止规则
  这一项变量。

### 调整内容

- 对于询问知识库是否包含某项内容的问题，只允许调用一次 search。
- 首次 search 未找到足以支持回答的候选时，立即提交 insufficient，且 evidence_ids 为空。
- 提交 insufficient 前后都不再调用 search、glob、ls 或 read；该规则优先于一般知识题规则。

### 状态

- Prompt-V1.7 已写入并保留，但不再作为当前生效版本。
- 超范围题一次 search 后停止、版本号和版本记录三项离线测试均已通过。
- Prompt-V1.7 真实冒烟失败；没有运行完整 10 题基线。
- 当前生效版本已恢复为 Prompt-V1.6。

### 真实冒烟结果

问题：`这个知识库是否说明了量子计算机的工作原理？`

- 最终返回 `answer_type=insufficient`、`citations=[]`，答案形式安全。
- 工具轨迹包含 5 次 `search` 和 2 次 `glob`；第 7 次调用状态为 error，说明模型没有遵守
  “只允许一次 search”的规则。
- 总 Token 为 33,139；Prompt-V1.6 同题为 30,071，增加 3,068（约 10.2%）。
- 离线 Prompt 契约测试只能证明规则文字存在，不能证明真实模型会遵守该规则。

### 本轮结论

- Prompt-V1.7 的实验假设不成立，属于反向优化，不应继续作为当前版本。
- 当前 `search` 总会返回 Top-K 候选，没有提供明确的“无可信候选”机器信号；模型因此继续改写查询或
  转向 glob。继续增加同义 Prompt 规则缺乏依据。
- 保留 Prompt-V1.7 内容与测试作为失败实验记录，不覆盖或删除；当前版本回退到 Prompt-V1.6。
- 下一步先比较超范围题与正常知识题首次 search 的候选分数，再决定相关性停止信号或运行时护栏；
  不为了回退而创建内容重复的 Prompt-V1.8。

## Prompt-V1.8：文件内内容定位实验（2026-09-10）

状态：工作树实验版本，真实冒烟未通过验收，不视为已修复。此版本新增内容定位规则，不是 V1.7 回退的重复版本。

### 问题与调整

- 原轨迹 glob 后从第 1 行连续分页，6 次成功调用后第 7 次被限制，最终 insufficient；总 Token 79012。
- 新增：只有文件路径但没有目标内容位置时，先以文件路径限定 search，再按返回行号 read。
- 保留直接 read 的例外：已有可靠目标行号，或用户明确要求通读全文。
- 将 search 范围规则从目录扩展为工具已返回的文件或目录；不改变工具实现、6 次上限、停止逻辑和结束原因展示。

### 验证

固定 evaluation/demos/patent_review_comparison_001.json 两事项题；同一索引、模型配置与运行时，三次独立 thread_id。未改正式三事项 Pilot。

离线：test_agent.py、test_agentic_evaluation_runner.py、test_agent_stream.py 共 35 passed。

| 轮次 | 轨迹 | 最终类型 | Token | 单题耗时 |
| --- | --- | --- | ---: | ---: |
| 1 | search → read × 3 | knowledge | 23100 | 22.13 秒 |
| 2 | glob → read → search → read × 3 → search 被拒绝 | insufficient | 67592 | 10.06 秒 |
| 3 | glob → read × 5 → read 被拒绝 | insufficient | 64077 | 8.02 秒 |

第 2、3 轮仍从文件开头读取，说明软规则不稳定。第 2 轮已读到两事项目标范围，但继续 search 后超限，属于另一个停止问题。第 1 轮返回 knowledge 不等于完整正确性验收，未做独立答案评分；成功回答输出更长，耗时不应与快速失败直接比较。

原始流事件保存在本地忽略目录 data/demo-diagnostics/v18-location-*.json。没有覆盖正式基线；三次小样本不构成性能统计结论。下一步应处理定位约束与工具契约的衔接，不继续把提示词存在测试当作行为保证。

## Prompt-V1.9：单文件 grep 定位实验（2026-09-10）

状态：实现与离线回归通过，真实 Demo 尚未验收通过；当前工作树保留候选版本，未提交推送，不视为稳定演示版本。

### 本次假设及边界

当问题提供明确业务名称时，字面 grep 可能帮助 Agent 从已找到的长文件直接定位正文，减少从头分页。不是为了证明 grep 必然优于 search，也不强制模型必须调用 grep。

- 新工具仅接受单个 Markdown 文件的 `path`、1～10 个 `patterns`、`limit`（默认 10，最大 20）。按字面 OR 匹配，不使用向量库。
- 复用安全路径映射与 read 的原文行号；返回 `matches`（path、line、text）和 `truncated`。长命中行围绕命中词返回最多 300 字符，并标记 `text_truncated`。
- 保留目录页及正文命中，由模型选择位置；不硬编码文件名、业务名称、章节行号或金标答案。
- grep 只定位，不登记证据。空命中不会触发 search 的 none 停止中间件。
- grep 纳入原有六次工具预算、轨迹提取和流式前端；原来的四工具顺序保留。
- V1.9 从 V1.8 完整快照产生，只增加 grep 能力介绍及对应的文件内定位选择。停止策略、六次上限、read 行数上限、模型、索引及 Demo 题面不变。没有采用此前未获确认的整套提示词改写。

### 离线与界面接线验证

- 初始新增后端测试 `14 failed`，前端 `1 failed, 5 passed`，均为预期能力缺失。
- 最终完整 Python 回归：`316 passed in 62.84s`。
- 前端 Node 回归：`7 passed`，覆盖 grep 开始/完成、关键词显示、命中行计数、截断提示、非法参数不会破坏页面以及已有流解析行为。
- 新增真实 Agent 循环的离线测试：六次 grep 可执行，第七次被拒绝；GroundedAnswer 不计数。
- 原文定点核验：真实指南可匹配目录第 26 行、正文标题第 210 / 284 行。此核验不是模型自主选择工具的证据。

### 同题 A/B 真实对照

- A：冻结 V1.8 + 原四工具；B：V1.9 + 五工具（含 grep）。运行顺序 A/B、B/A、A/B，每次新 thread_id；共用只读既有索引和模型配置，不复用对话消息。
- 模型 `qwen3.6-flash`，temperature=0，关闭思考；工具上限 6。延迟从单题流执行开始计时，不含运行时初始化。
- 问题固定为 `evaluation/demos/patent_review_comparison_001.json` 的两事项比较题，未修改正式三事项 Pilot 或其评测文件。
- 原文 SHA-256：`c94b6b1d12e9aace29f218e793fb574d5c1b1ad74c2930892c7d309e560bf2d6`。
- 读取覆盖只在运行结束后按 Pilot 中对应两事项的 8 个答案点行范围统计，不把金标提供给模型；覆盖行范围不代表答案完整正确。

| 运行 | 主要行为 | 读齐答案点范围 | 最终类型 | Token | 延迟 |
| --- | --- | ---: | --- | ---: | ---: |
| A1 | glob 后从第 1 行逐页读，第七次 read 被拒 | 0/8 | insufficient | 45350 | 14.88 秒 |
| A2 | search 定位后分页读齐，第七次仍 read，被拒 | 8/8 | insufficient | 54817 | 14.43 秒 |
| A3 | glob 后从头分页，第七次 read 被拒 | 4/8 | insufficient | 51724 | 13.57 秒 |
| B1 | 未用 grep，从头分页，第七次 read 被拒 | 7/8 | insufficient | 69625 | 18.35 秒 |
| B2 | 从头分页已读齐，第七次才发起 grep，被拒 | 8/8 | insufficient | 82318 | 20.89 秒 |
| B3 | glob → read 1–80 → grep → read 210–289 → read 290–369 → 回答 | 8/8 | knowledge | 46798 | 20.50 秒 |

A 平均 Token 为 50630.33，平均延迟 14.29 秒；B 为 66247，19.91 秒。本轮 B 总体成本未降低，不能用 B3 单个样本宣传性能改善；两组最终输出质量也不等价，且样本数很小。

### 结论与未通过项

1. **观察到一次自主 grep 定位并在五次调用内结束。** 但 B 的三次中仅一次实际执行了 grep；另一次请求已在预算之外，因此尚未证明稳定性改善。
2. **定位和停止仍须分开。** A2、B2 均已覆盖全部目标范围，随后还有一次调用并超限；不能把这些失败归因为没有找到内容。
3. **B3 不算合格答案验收。** 两条引用的 path/line/quote 都能回源，但只引用第 210、284 行的章节标题，不能支撑表格中的条件、材料和流程。引用可复现不等于引用充分。
4. B3 总结中的“必须先经保护中心预审合格后方可提交国知局”还遗漏了原文第 326 行允许未通过预审后走普通程序的分支。未运行额外语义 Judge，本轮不宣称答案准确率提升。
5. 本轮保持实验原样，不继续增加关键词映射、不提高预算、不在失败后反复调 Prompt 直到出现成功。停止策略与引用充分性问题留待下一轮单独确认。

复跑脚本在本地忽略目录 `data/demo_grep_ab.py`。完整题面、Prompt、模型配置、原文 hash、事件和单题指标保存在 `data/demo-diagnostics/grep-ab-c93e2fa6a52d48ac8e237c39bc8379a9/round-{1,2,3}-{A,B}.json`，汇总为同目录 `summary.json`。未覆盖正式评测结果。

## V1.9 + locate_first：可配置运行时策略实验（2026-09-10）

本次不是新 Prompt 版本，V1.9 文本保持不变。用户确认当前 Demo 先采用无行号内容问答的定位优先流程，并要求不得针对题目硬编码。

### 实现边界

- `AgenticRuntime.retrieval_policy` 默认 `free`，其他调用方保持原行为。Demo 的 JSON 配置显式设置 `retrieval_policy: locate_first`，改回 `free` 并重启即可恢复。
- 策略只检查当前用户轮次中是否已有成功执行的 search/grep。此前从模型工具名单中移除 read；执行端也拒绝提前 read，不能只靠工具 Schema。
- 完成一次定位尝试后恢复 read，包括 grep 空命中；不判断语义相关性，不建立行号白名单，不限制后续上下文和分页。工具参数错误不算成功执行；search none 仍沿用原停止中间件。
- 每个新用户轮次重新判断。没有题目 ID、文件名、关键词或固定行号的条件判断，没有额外的 LLM 分类调用。search/grep 选择及所有检索参数、read 行号仍由模型生成。
- 六次工具预算、提示词、语料、索引、答案结构和引用闸门均未调整。只在当前 Demo 配置中启用，不做通用自然语言意图分流。

### 验证

完整回归 `329 passed in 60.22s`。新增测试覆盖默认关闭、任意题面配置、同步/流式入口传参、同步/异步中间件、执行端拒绝、跨轮重置、grep 空结果恢复，以及真实 Agent 图里的工具绑定变化。

以下三轮没有脚本预执行工具或注入位置，全部调用由模型发起。两事项金标仅用于运行结束后的范围覆盖统计。

| 轮次 | 实际流程 | 目标证据范围 | 最终类型 | Token | 耗时 |
| --- | --- | --- | --- | ---: | ---: |
| 1 | glob → grep → read 210–289 → read 290–369 → 回答 | 8/8 | knowledge | 29696 | 41.08 秒 |
| 2 | glob → grep → read 210–289 → read 290–369 → 重复 read 284–293，最终第七次被拒 | 8/8 | insufficient | 51809 | 11.27 秒 |
| 3 | glob → grep → read 210–289 → read 290–369 → read 370–389 → 回答 | 8/8 | knowledge | 46560 | 47.15 秒 |

三轮均主动调用 grep，首次 read 均从正文第 210 行开始，没有从第 1 行顺序翻页。关键词并不完全相同：前两轮为“专利优先审查 / 快速预审”，第三轮为“优先审查 / 快速预审”；limit 也由模型选择。

平均 Token 42688.33，平均耗时 33.16 秒。历史自由 V1.9 三轮为 66247 Token、19.91 秒，但不是同期随机对照，且本次两轮生成了更长的结构化输出（输出 Token 3643/6748、引用 35/81 条），不能宣称稳定降本或提速。

### 验收结论

- 当前样本的“先定位再读”顺序改善，但不能保证所有题目都能找到相关内容。
- 停止问题未解决：第二轮读齐后重复相同范围并超限。第三轮读齐后还有一次额外读取，仅记录，不自动判为违规。
- 完整答案尚未验收：已输出答案仍补充原文未列举的“节能环保、新一代信息技术”等法规示例；引用条数较多也不等于每个结论都有充分证据。未调用额外语义 Judge。
- 保留为本地候选策略，未提交推送。不得把程序的顺序约束解释为模型自主规划能力本身已提升。

原始记录在 `data/demo-diagnostics/policy-locate_first-4243c3ed8a1642d5a2c315e044f58052/`，含 `round-1.json`～`round-3.json` 与 `summary.json`；复跑脚本为本地 `data/verify_locate_first.py`。现有正式评测文件未覆盖。

## V1.9 + locate_first：预算耗尽后的最终答复阶段（2026-09-10）

用户要求回到首要问题，不扩展诊断框架。本次只修已证实的顺序调用路径：工具预算用完后，模型仍尝试第七次工具，Agent 被结束且没有 structured_response，已有原文因此无法形成答复。

### 改动

- 在现有 KnowledgeToolCallLimitMiddleware 的模型调用前读取父类实际 run_tool_call_count；达到其 run_limit 时撤下知识库工具，保留 response_format。当前 LangChain ToolStrategy 会继续绑定 GroundedAnswer。
- 不增加六次工具预算、不按题目/关键词/行号判断，也不注入答案。模型可基于 read 证据回答，也可选择 insufficient。证据 ID 与原文校验不变。
- 预算未满时保持原流程；父类执行端上限仍保留。没有把超限行为改为无限 continue，也没有追加自动读取或重建索引。
- 使用实际运行计数而非历史消息条数，兼容诊断中“两次预执行 + 剩余四次”的场景；同步与异步模型调用一致。
- 这不是提前判断“已经读齐”的语义规则，也不能处理所有异常调度场景。单个响应并行提出超出剩余预算的多项调用仍沿用父类机制，本轮未重写调度器。

### 验证

新增预算收口测试先 4 failed；实现后完整回归 `333 passed in 47.88s`。真实 Agent 图测试确认：六次运行预算用完后仅绑定 GroundedAnswer；预提供历史的四次剩余预算也不会被提前扣完。测试同时保留 insufficient 作为有效最终答复，未强迫生成 knowledge。

真实请求通过同一个 POST /demo/pilot/stream 路由在独立进程执行，V1.9、locate_first、模型、语料均不变，无脚本预执行工具或注入位置：

| 轮次 | 知识库工具调用 | 工具错误 | 原始结果 | 校验结果 | Token | 耗时 |
| --- | ---: | ---: | --- | --- | ---: | ---: |
| 1 | 4 | 0 | GroundedAnswer / knowledge | 通过 | 26712 | 29.44 秒 |
| 2 | 6 | 0 | GroundedAnswer / knowledge | 通过 | 64499 | 34.16 秒 |
| 3 | 6 | 0 | GroundedAnswer / knowledge | 通过 | 51579 | 41.47 秒 |

本轮两次实际用满六次工具后均形成结构化答复，没有第七次工具超限丢答。第2轮的路径仍为 search 后多次 read，说明预算收口与检索选择是不同问题。

### 尚未解决

- 仍有多余读取；本次不宣称 Agent 会在刚读齐时立即结束。
- 第1轮只引用章节标题，第2轮只引用“（一）受理条件”两个小节标题；第3轮有26条引用，但仍添加原文未列举的法规示例。结构与来源校验通过，不代表答案与引用充分性验收通过。
- 用户旧 Chrome 第三次“六次工具全成功但拒答”缺少后端原始记录，原因仍未确定，不能把本次修复解释为已还原那次历史请求。
- 三次小样本不构成成功率或延迟保证。没有扩展生产诊断接口，也没有覆盖既有正式评测文件。

新原始记录：`data/demo-diagnostics/finalization-probe-30b40c53a8e9443785d1ec283bcb86ad/run-1.json`～`run-3.json`，其中保留原始模型消息、structured_response、证据校验分支与最终结果。

## V1.9 + locate_first：路径发现进展护栏 V1（2026-09-14）

运行期规则版本：`locate-first-progress-v1`。静态 Prompt-V1.9 不变，六次预算不变；本次只修改可选策略的发现→内容定位衔接，不创建内容重复的新静态Prompt版本。

### 已确认问题

固定当前题面五轮中，两轮均为 `glob×2 → ls×4 → insufficient`。文件路径已找到，第三次调用时还有四次预算，search/grep实际可用，但目录查询连续重复；两轮原始模型已经拒答，不是来源校验降级。

原始问题清单：`data/demo-diagnostics/current-five-runs-20260914T053115Z-a5ad6296/CURRENT_ISSUES_V1.md`。不覆盖该批结果或修改Gold。

### 本次改动与边界

- 从当前用户轮的模型调用及成功ToolMessage推导已发现文件、已成功的目录查询和因重复被暂停的工具；不在中间件实例上存跨请求状态。
- 已找到文件但尚未完成search/grep时，模型请求临时附加阶段说明和有界的真实返回路径数据。原SystemMessage通过复制扩展，不写入对话历史、不替换原规则。
- 首份命中不会立即关闭ls/glob，正常不同参数仍可发现第二份资料，串行与同批并行均保留。
- 此阶段已成功、非空的同参ls/glob再次调用时，执行端返回`duplicate_discovery`，不执行目录查询。根路径省略/显式传入、合法尾斜杠、target首尾空格按有效参数判重。
- 出现上述重复后，那个工具名临时撤下，直至本轮一次search/grep成功执行；成功空grep也恢复。重复glob后不同target也暂不可见，这是违规后的短暂限制，不是所有路径发现被永久关闭。
- 空/错误发现结果不视为成功非空查询，允许恢复；新HumanMessage重置全部进展。默认free不改变。此护栏假设本轮路径发现结果稳定，不代替实时目录监控。
- 被拒模型调用仍占预算、保留error轨迹，不退款，不修改已有预算中间件；预算用尽tools=[]时不补回工具或追加必须定位的提示。
- 没有题目ID、文件名、关键词或固定行号的条件分支。search/grep选择、参数和read位置仍由模型决定。

### 离线验证

新失败测试先得到7 failed、6 passed；实现后补充新资源发现、空结果、调用ID关联、工具字典格式、系统内容块、同步/异步及真实Agent图测试。最终完整后端回归：363 passed。

真实图确认重复目录请求不会再次执行，拒绝后定位及read仍可恢复；第六次被拒仍计六次预算，后续只绑定GroundedAnswer。free保持原执行行为。

### 真实验证与未完成项

同一固定任务、同模型/原文/索引、六次预算独立验证三轮，目录：`data/demo-diagnostics/current-3-runs-20260914T060353Z-207e5195/`。三轮均为`glob×2 → grep×2 → read教师×2`，均进入内容定位、无重复发现调用、无工具error；两轮knowledge、一轮模型主动insufficient。

| 轮次 | 知识调用 | 重复发现 | 原始答复类型 | 两份必要正文覆盖 | Token | 耗时 |
|---|---:|---:|---|---|---:|---:|
| 1 | 6 | 0 | knowledge | 否，未read学生 | 30446 | 26.14秒 |
| 2 | 6 | 0 | knowledge | 否，未read学生 | 31501 | 26.68秒 |
| 3 | 6 | 0 | insufficient | 否，未read学生 | 29728 | 9.91秒 |

真实三轮未触发重复调用的执行端拒绝，说明观察到的是路径→内容定位行为收敛；硬拒绝及退款不发生的性质由真实图离线测试证明，不能单独归因哪个组件带来变化。小样本不构成总体稳定性保证。必要正文覆盖未改善，不将knowledge计为完整业务成功。

本次不解决多资源read覆盖、逐题分类或Citation语义支持；这些分别进入后续独立步骤。原始诊断数据仅保留在本地，不随代码修复提交。
