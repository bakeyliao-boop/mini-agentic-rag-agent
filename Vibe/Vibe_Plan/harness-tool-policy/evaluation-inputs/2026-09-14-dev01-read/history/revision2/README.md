# DEV-01真实工具记录转评估输入

状态：材料整理完成，待审核；未调用模型，未确定评价次数/费用，未生成策略。

- 审核重点.md：给用户看的最小片段及位置。
- input.json：评估器单例输入；保留前序glob、grep及本次read完整实际返回。
- request.example.json：固定v3规则正文+单例input的请求样例，未发送。
- review-reference.json：组织侧拟定结论，不进入模型请求。
- provenance-and-checks.json：来源哈希、事件/HTTP对应及静态核验，供组织者回查。

只复用dev01-20260914-02已有记录，不重跑Agent、不查索引。题目参照来自已确认的DEV-01 v2，金标位置未进入task_reference；实际工具返回的行号、ID保留。原始来源哈希放在追溯文件中，不能误称这些哈希都由工具返回。

真实多行read保留20行实际返回和10条逐行登记关联，不沿用模拟案例的单个可引用布尔标志。登记依据为实际返回ID，已对照该运行冻结的tools/evidence代码和read正文；并非凭工具名称推测全部行已登记。输入的record_context说明核验来源与完整性边界。

v3规则原字节不变。其中synthetic描述只适用于模拟场景，本输入origin明确为real_agent_run，不将实际事件说成模拟；模型对该真实结构的适配尚待试用核验，静态构造不等于运行时效果通过。

后续先核对材料，再单独批准评估器调用；不把组织侧预期或后来成功答案加入请求。即使read判断有贡献，也不能自动授权策略修改，且本次定位过程没有已证实的需改问题。


## revision2更新

依据用户转交复核，已将四处组织核验过程文字移出input并更新请求及哈希；完整返回和登记关联不变。准备/评估新增调用计数已分别明确。详见[复核意见与修订记录](复核意见与修订记录.md)。原件保存在history/revision1；三项参照仍待用户确认，尚未送评。
