# DEV-01首次真实运行准备

状态：准备草案，未获本次付费执行或DashScope凭据复用授权。未运行Agent、重建或复制索引。

## 建议任务

使用question.txt中的v2统一要求和DEV-01单道题面，原策略独立运行一次。执行模型只拿该文本、原系统提示词及工具；不传题号用途、参照答案、目标行号、已知路径或推荐路线。D01为唯一知识语料，不使用D02/VAL/REG。

本次目标是获取可审核的真实过程；合理执行保留为正面参照，不强求制造问题。有真实且值得改进的反馈后，再讨论DeepSeek候选。

## 已核对

当前工作目录没有data索引目录；原项目data/pilots/multi-chunk-guide-001存在knowledge_chunks索引。SQLite以mode=ro读取，86个条目文本逐一与当前冻结D01对应行区间一致。见index-readonly-check.json。尚未核实生成向量所用模型和维度，不能仅凭正文一致宣布检索可复用。

现有Pilot运行时可接已有索引，但传入的原Pilot大题和答案不用于本轮。后续只复用D01运行环境，输入用question.txt。不得直接走会建索引的通用runtime构建入口。Chroma打开可能写状态，应先在授权范围内复制完整索引到本工作目录，再验证；原库保持只读。

当前代码执行模型默认qwen3.7-flash、temperature=0、enable_thinking=False；.env.example仍有旧模型示例，本轮未改模板，不能按旧示例推断实际型号。

## 费用与运行条件尚需收口

[官方Qwen3.7-Flash价格](https://help.aliyun.com/zh/model-studio/qwen3-7-flash)，2026-09-14核对：北京输入不超过32k时输入0.2元/百万Token、输出0.8元/百万Token；长输入有更高阶梯。实际地域端点未核对，不把该费率直接当本批已批准费用。

一次Agent运行可能多次调用Qwen，search还可能调用查询Embedding；不能按一次LLM调用算整题。六次知识工具限制不等于六次模型请求。当前模型工厂未显式设置输出上限、timeout、max_retries，尚不能给出可靠的硬费用上限。

建议下一步先固定本批模型及Embedding连接来源、无自动重试和费用保护方案。任何新增外部中断上限必须单列：触发后记实验中断，不得当作Agent正常停止，且未来原版/候选一致。候选策略仍不修改六次知识工具预算和既有停止规则。

先保存真实运行，再核对记录并整理评估器输入；DeepSeek离线评价和策略生成分阶段授权，不在本次准备中自动调用。新记录需保留真实缺失，不能直接复制CAL模拟场景布尔值。

## 尚未执行

未加载任何.env，未发模型或Embedding请求，未调用DeepSeek，未改代码、创建索引或Git提交推送。实际题目选择、运行费用与连接复用尚待确认。
