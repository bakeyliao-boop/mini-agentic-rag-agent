# DEV-01单次真实运行执行范围（待授权）

目标：原策略执行DEV-01一次，留下真实记录供人工审核。不包括DeepSeek评价、候选生成或效果对照。

## 索引与连接

只读SQLite已证实86个正文片段与D01对应，集合1024维；86个FLOAT32向量各4096字节。未保存Embedding模型来源，不能将.env.example当历史证明。

建议授权后先对原Pilot目录作本地一致性复制，冻结复制文件哈希，在工作目录运行，不写原库。用text-embedding-v4、1024维对一条已有原文做一次向量核对，按相同全文输入比较旧向量。吻合只能作为抽样兼容证据，不宣称全索引来源已证明；不吻合或缺有效返回则暂停，保留费用与记录，不自动重建。具体数值容差在请求前固定并写入批次；不得看了结果再调阈值。

仅复用原项目C:/Users/hyf/Desktop/mini-agentic-rag-agent/.env中的DASHSCOPE_API_KEY与DASHSCOPE_BASE_URL，不打印或复制密钥。拟限定北京官方兼容端点；不匹配则暂停，不自动套用北京费率。Embedding模型/维度和执行模型明确固定，不依靠模板默认。

## 原策略运行

- question.txt原样作为统一要求+单道题面；题号与人工参照留组织侧。
- qwen3.7-flash，temperature=0，enable_thinking=False，单次输出上限2048。
- 五种知识工具全部保留，原系统提示词、read上限与六次知识工具预算、search none停止和引用校验均不改。
- 沿用recording_dir保存真实返回与模型回调；同步留存计费请求次数、API用量及外部中断原因。需补实验入口的独立费用保护，不能依靠best-effort记录回调执行硬拦截。
- 无自动重试，最多8次Qwen请求；最多1次索引兼容核对+6次search查询Embedding请求，每次Embedding限一个输入、至多8192 Token。模型结构化收口不算知识工具，但属于Qwen请求计数。
- 单请求超时120秒，整题建议10分钟外部期限。取消不能保证服务端未计费，未知费用保留预占。限额触发记实验中断，不能评价为Agent本身停止不当；未来原版候选保持相同保护条件。

次数上限8是实验费用保护建议，不是理想工具路线或已验证足够的业务阈值；触发后不补跑到成功。

## 保守预算

建议总授权上限10元，实际预计远低于该上限，但暂不以未计数的动态上下文承诺具体实扣。

采用北京公开费率最贵输入档，按每次最多100万输入Token、2048输出Token且不命中缓存，8次Qwen最保守计算：8×(1.2+2048×4.8/1000000)=9.6786432元。

Embedding预留7×8192×0.5/1000000=0.028672元。两项合计9.7073152元，低于10元。这是采用模型极端上下文上限的费用预占，不是预测会花近10元；实际短上下文按对应更低档及返回usage计算。若需要更低授权上限，可另定输入Token上限并实现发送前精确核算，不把估算当硬上界。

价格来源（2026-09-14核对）：https://help.aliyun.com/zh/model-studio/qwen3-7-flash ，https://help.aliyun.com/zh/model-studio/text-embedding-v4 。北京Qwen<=32k档0.2/0.8元每百万输入/输出，高档1.2/4.8；Embedding文本输入0.5元每百万。最终账单未核验不能称实扣。

## 执行前完成与验证

费用保护入口、凭据/端点边界、取消与零重试、请求计数、复制索引一致性及向量核对标准须在真实调用前完成离线验证与材料冻结。离线不发模型或Embedding请求。任何校验失败暂停，不自动扩大范围。

拿到轨迹后先人工检查输入隔离、工具返回、登记与答案引用；再讨论向评估器传哪次真实调用。先观察，不承诺一定发现可改进问题。

当前仅完成这份可审阅方案与SQLite只读核验；以上代码接线、索引复制、向量核对和真实执行均尚未进行，不由之前“同意准备方案”推定授权。
