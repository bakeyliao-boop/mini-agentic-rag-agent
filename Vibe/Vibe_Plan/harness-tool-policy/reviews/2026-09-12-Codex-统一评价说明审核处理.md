# Codex统一评价说明审核意见与处理

日期：2026-09-12。状态：独立审核意见已由用户转贴接收；非阻断表达建议已落实为v2，v2未另行独立复审；没有执行评估模型。

## 来源和接收方式

用户在当前对话分段转贴报告，并明确“发送完毕”。来源按本次指定Codex线程审核交接上下文登记，目标审核线程为01a08ac9-ce7e-70b0-8225-dedc1e2800f6，请求方为01a08912-69ea-7ac0-8d60-2144d7d08fc6。报告通过用户转贴收到，不是自动跨线程回传成功。

此前直接消息与CLI接续失败保留在交接文档，不覆盖历史。本次用户提供的报告未包含完整审核版本哈希或末尾操作清单；本记录不补造该审计回执，也不声称审核者没有执行任何未列操作。其引用的统一说明L54及输出示例位置与本地v1相符。本轮另核对本地快照及静态证据，见下文。

以下为转贴内容的结构化整理及主线程判断，不是逐字全文副本。

## 审核结论及独立判断

审核方陈述：“本次未发现必须修改的实质问题”，仅一项低优先级、非阻断改进。五例输入、规则、输出示例一致；隔离边界明确，但尚未验证真实模型请求。

主线程判断：接受。规则v1 L54要求前后各附摘录，而CAL-03/04主要给出位置和概括，确有表达口径歧义；允许同源同文共用最短摘录可以消除不必要重复，同时保留逐侧定位和可回查性。该修正不涉及评估目标或事实判断，不需要增例、冻结输出API或启动新一轮实验。

## 意见处理

| 优先级 | 意见 | 处理与范围 |
| --- | --- | --- |
| 低，非阻断 | 前后“各附”短摘录可能与CAL-03/04示例不一致 | 统一说明另存v2：前后各给输入位置；关键文字最短摘录，同源同文可只摘一次且关联两侧事件；无正文或历史缺失不补造摘录。 |
| 同项配套 | 示例需要体现上述简洁表达 | CAL-03/04增加共用短摘录“提交办理请求一个月内”及两侧关联；CAL-04仅该关键文字相同，不声称两侧完整返回相同。CAL-01/02共用展示实际路径，CAL-05补当前短摘录。 |

## 五例对照（审核方结论，经本轮材料复核无实质冲突）

| 案例 | 当前事实支持 | 事实新增 | 支持必要事实的可引用增量 | 关键理由 |
| --- | --- | --- | --- | --- |
| CAL-01 | 充分 | 是 | 是 | 完整前序的实际路径不含期限，本次正文补齐并登记。 |
| CAL-02 | 不充分 | 否 | 对必要事实而言，否 | 标题虽登记但不含期限；保留定位线索价值，不判整个调用无用。 |
| CAL-03 | 充分 | 否 | 否 | 同版同文此前已可引用，无新疑点等条件下没有新增必要支持。 |
| CAL-04 | 充分 | 否 | 是 | 摘要已含事实、路径、版本和行号，本次按案例规则补齐引用登记。 |
| CAL-05 | 充分 | 无法判断 | 无法判断 | 当前支持及登记已知，缺失前序使增量未知，不等于此前无调用。 |

no_for_required_fact保留必要事实范围；necessary_source_verification不泛化成所有系统必须read。event_id不混称证据ID。业务参照不计作Agent实际所得，工具返回位置不等于金标位置泄漏；登记状态与历史完整性保留为场景前提，不能单独代替内容判断。

## 输入、追溯和未验证范围

- 未来被测请求仅单例input及统一规则正文；示例、预期、人审状态、完整README、审核报告、全量JSON不进入请求。业务参照中无金标source_line，实际返回位置可保留。
- 四项输出栏目（最低必要内容、前后内容、三项判断、贡献范围）适度；理由错误不能因标签相同而被忽略。
- 五例同一必要事实、相近正文，可能被字符串、工具类型或元数据规则恰好判对。禁止捷径的文字不证明实际评估器没有走捷径。
- 搜索可直接引用、多片段登记、残缺历史中的明确前证、版本/矛盾/数据内指令等扩展规则尚未被本组覆盖，不构成强行增例要求。
- 没有最终答案、最终引用集合或停止事件，不能评价其正确性、利用、停止原因、最优路径或整体策略效果。真实记录兼容性和逐段登记关联尚未验证。
- 同组材料用于调规则后不再作为独立泛化证明。模拟案例不能当作mini-agent真实失败反馈，更不能据单条贡献标签直接决定改提示词或继承策略。

上述局限均接受并保留。后续仍是获准真实运行反馈、原因分析、受限候选、对照验证与人工继承的主线，本次不进入模型执行阶段。

## 本轮版本与静态核验

| 文件 | 本轮读取或生成的SHA-256 |
| --- | --- |
| 统一说明v1（保留） | `e867c6b2eaa3d27ce9af9e684566007c6fcfabb44a8fb64354e2615e089a76cc` |
| 输出示例v1（保留） | `3ffc0a9fdcecb5a8316ac9c9efd7dbac06781f4ab5f47b8fda0ab9c7e3baf804` |
| 统一说明v2 | `f3a3a87413da951fbcdfe1e7c00e2483daf56dd463366bebf9e4bed9048db695` |
| 输出示例v2 | `f029676a36aac685237bce812b9c7752ee6d22e504a673d46fadc8a250385523` |
| cases.json（修订4，未修改） | `5c69d01ae272b19ae4510ca76f45cd4edc6875404a4d4bafc714593174591451` |

本轮核对6条完整原文行、1条摘要子串及新增共用短摘录；v2中27处输入路径均可解析。v1/v2题集、冻结来源、cases.json、两份说明v1及既有tracked diff均保持原样。所有actual_evaluator_result仍为null，人审标记不变。这是材料静态核验，不是项目测试或模型校准结果。

实际新增：两份说明v2及本文。追加更新：校准README、规划入口、交接接收状态、静态核验记录、ADR-0004和讨论档H-035。未改代码或配置，未调用项目模型API、建索引、生成策略、提交推送。

见[统一说明v2](../calibration/2026-09-11-v1/评估器统一说明草案-v2.md)、[输出示例v2](../calibration/2026-09-11-v1/评估输出示例与对账说明-v2.md)。
