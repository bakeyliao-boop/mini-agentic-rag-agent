# 一次受限格式补交：本地修复与离线验证

2026-09-14，用户要求按本地修复方向尝试。已实现并完成离线验证；没有新增真实模型调用。此处不宣称真实Qwen补交已成功。

## 行为

1. 已有合法GroundedAnswer保持原流程，不补交。
2. 缺structured_response且最后一条是明确finish_reason=stop的普通AI回复（无有效/无效tool_calls）时，识别输出协议错误。关闭恢复时抛OutputProtocolError，避免返回业务insufficient。
3. 同步/流式运行入口可显式开启recover_output=True。模型须明确max_retries=0；使用原对话、原系统要求及一条通用补交说明，仅绑定GroundedAnswer并指定该工具，调用一次。没有知识工具执行器，不重新检索，无金标输入。
4. 补交必须只有一个GroundedAnswer调用，schema有效；knowledge必须有本轮有效证据ID，原文核验通过。才进入现有最终交付。失败抛明确OutputProtocolError，不循环；真实insufficient结构化回答仍可作为业务结果。

预算/检索触发的工具消息或无正常完成元数据的合成消息不走这次恢复，避免绕过既有停止规则。当前修复针对本次已观察的正常stop纯文字缺提交情形，不承诺所有供应商格式异常都覆盖。

新增rag_core/agentic/output_recovery.py；runner.py接入可选恢复。未改变Agent原系统提示词、定位策略、read范围、工具预算或模型schema。recover_output默认False，不给既有调用自动追加付费请求。显式启用恢复的运行，新增最多一次逻辑模型调用；要求零SDK重试并沿用实验传输预算。实验外无预算约束时不得声称自动受本批费用上限保护。

成功恢复的reply加入用量累计；工具轨迹不追加检索。启用记录时单独保存恢复开始、回复、成功/失败事件；失败回复和用量仍可在原始事件中回查。同步失败表现为明确异常，流式演示现有异常通道接收错误，不伪装completed知识回答；未重新设计Web错误页面。

## 离线验证

- output_recovery、agent_runner、agent_stream、run_recording相关检查：33 passed in 35.53s。
- 补加零SDK重试检查后，output_recovery、agent、grep检查：61 passed in 35.63s。
- 两轮有14项重复，合计80个不同检查通过。git diff --check通过。

关键验证包括：不重复查询；只允许GroundedAnswer；再次纯文字、错误工具、schema错误、假证据ID、源文变化、截断及请求异常均不无限重试；已有结构化结果/工具停止/预算消息不触发恢复；同步和流式结果包含补交用量。

针对本次DEV-01：使用原4条已保存HTTP响应，经真实SDK和Agent图离线回放。第5条合格补交响应由测试人工构造，包含当前read返回的真实本轮ID。验证结果为knowledge、L489引用、仍3次知识工具调用。第5条是模拟数据，不是Qwen实际输出，不计作真实策略/模型效果。

## 真实验证准备

execution/2026-09-14-output-recovery-preparation/request.example.json由原最后一次HTTP输入、实际纯文本回复和通用补交说明组成；工具仅GroundedAnswer，没有新检索或金标。manifest固定来源及请求哈希，execution_authorized=false。

建议仅发送这一条真实Qwen补交请求，零重试、输出上限2048，按极端上下文费率保守预占1.2098304元，提议授权上限1.30元（非实际费用预测）。需用户另行批准该付费范围及指定连接复用；不重跑整题、不建库、不调用DeepSeek。

已将修复前runner精确归档到准备目录runner-before-fix.py，并核对与原执行批次冻结哈希相同。旧失败请求、原始响应和测试结果保持不变。未提交推送。