# 保持v3原样的成对诊断材料

日期：2026-09-14。状态：已准备，未运行。用户建议先测试，再决定是否修改措辞；本包保持v3完全不变。

| 输入 | 唯一区别 | 预期三项判断 |
| --- | --- | --- |
| CAL-05 | 前序为空、历史不完整 | 充分／增量未知／引用增量未知 |
| CAL-05m1 | 历史仍不完整，但增加一条明确且可引用的同事实前证 | 充分／无事实增量／无引用增量 |

m1的前证复用旧CAL-03的e2读取事件，正文与来源不改。本次返回、题面、业务参照、固定版本/无矛盾等场景条件与CAL-05相同，仅prior_events从空列表改为这条明确前证。案例ID和预期在组织侧，不进入请求。

本包由已经讨论的CAL-05及变体构成，是诊断/回归，不是独立迁移验证。规则v3没有加入这两例及其答案。

建议两例各独立3次，共6次，继续使用deepseek-flash、非思考、temperature=0、2048输出上限，串行零重试；新批次预算上限建议1元，尚未批准，不沿用旧批次剩余额度。实际费用需在接线和计数核验后给出，不能沿用v2计数为本包实测。

若两类都按预期判断，暂不为通俗措辞改v3；若m1被一律判未知，才有实际证据支持进一步澄清局部状态与完整历史；若原CAL-05仍错，继续检查误判理由，不仅改词就宣称解决。两例通过也不能证明全部原五例回归或迁移验证通过。

现有执行入口限定旧五例/v2布局，不能直接运行本包，也不能伪装ID绕过校验。新批次运行前需要在明确范围内适配和离线验证，保留同等次数/费用/原始记录边界；本轮未改代码，未调用API，未读取凭据。

文件：cases.json为组织侧完整材料；两份request.example.json是实际请求正文样例；preparation-check.json记录静态差异检查。没有生成模型结果，execution_authorized=false。


## 执行更新

用户已授权本轮适配及6次测试。正式冻结材料位于../batches/calibration-20260914-v3-pair-01，6次核心判断符合预期，暂不改v3措辞；本目录仍保留准备阶段原样材料。结果见项目evaluation/results/tool-contribution-calibration/calibration-20260914-v3-pair-01/diagnostic-report.md。
