# 首轮真实校准候选批次：calibration-20260912-01

日期：2026-09-12。状态：运行包已备齐并通过零发送预检，等待一次最终确认；真实执行仍关闭。不是已经跑过的批次，也没有模型校准结果。

## 沿用已经接受的决定

- 模型：deepseek-flash，官方规划时对应V4.1-Flash；实际响应版本待运行记录。
- 范围：CAL-01至CAL-05各独立3次，共15次请求；无额外探针或自动重试。
- 总预算上限：1元人民币。按固定官方本地输入计数及拟议最大输出，计算约0.304元；仍保留每次12000输入tokens预留，总预留0.60576元。不是实扣或API上界保证。
- 初筛标准：过程可核对、15次评价完整、结论和理由正确；合理unknown和参照争议按已接受规则处理，不自动判语义合格。

## 这次最终确认覆盖什么

把此前逐例讨论、独立review认可的五例预期统一签收，并按下列固定参数开始这个唯一批次。模型、次数和预算不重复选择。

| 案例 | 当前必要事实支持 | 本次事实新增 | 本次可引用支持增量 | 限定 |
| --- | --- | --- | --- | --- |
| CAL-01 | 充分 | 是 | 是 | 完整前序实际只有不含期限的路径，本次取得期限正文并登记。 |
| CAL-02 | 不充分 | 否 | 对必要事实而言，否 | 只有收费标题，不含时间；不否定其他可能定位价值。 |
| CAL-03 | 充分 | 否 | 否 | 同版同文此前已可引用；没有新疑点或额外核实要求。 |
| CAL-04 | 充分 | 否 | 是 | 前序已有期限、出处和位置；本次仅按本例规则补齐引用登记。 |
| CAL-05 | 充分 | 无法判断 | 无法判断 | 当前内容及可引用性已知，前序缺失使增量未知。 |

固定参数：显式非思考、temperature=0、max_tokens=2048、非流式、文本输出、不提供工具；客户端串行、零自动重试、整体120秒截止。异常不自动补齐到15条成功结果，不改规则重跑到通过。

当前候选材料保持原case人审标记（CAL-01为true，其余false），参数与execution授权亦待最终确认。收到明确确认后，只在本候选包中写入本次审批记录、完整人审签收和执行允许元数据，并重新绑定相关哈希；题面、业务参照、工具记录、预期及实际请求正文不得变化。原2026-09-11-v1材料保留不动。

## 唯一输出与运行前检查

正式输出路径：`C:\Users\hyf\Desktop\mini-agent-harness-lab\evaluation\results\tool-contribution-calibration\calibration-20260912-01`。目前没有创建该目录；真实入口须使用这个完整路径及固定batch ID。已有目录不能覆盖，也不能换父目录重复执行同一授权。

已运行一次prepare，网络/凭据加载入口均设为一旦触发就报错：prepared、计划15、尝试0、响应0。当前仅待执行授权、参数接受记录、整例人审三个确认状态，其他路径、规则、计数和源哈希校验均已通过。见[预检回执](preflight.json)。

[审阅快照](review-manifest.json)记录所有核心文件哈希及实现哈希。五份请求正文、规则、案例内容和本地计数数据均复制自已核验版本；请求文件和单例input与原版逐项一致。

真实运行前再次核对正文、代码版本、输出路径、渠道与价格约束。若配置缺失或非已审官方渠道，入口拒绝，不自动换服务商。认证只由获准真实模式使用，不显示或存储密钥。当前没有读取.env或启动真实请求。

## 本批结束后交付

保留全部原始请求/回复/错误、结束原因、用量/费用及来源；人工逐项检查三维判断和理由，报告明确误判、缺项和参照争议。若未完成15次，明确已发及未发数量，不隐藏失败，不自动恢复或追加。

本批不能产生Harness策略候选，不改mini-agent策略，不用D02/VAL/REG，不建索引，不提交推送。初筛通过只支持后续受监督验证，不自动扩大范围或继承策略。


## 最终启动确认

用户已回复“确认”，统一签收五例预期、参数和本批最多15次真实运行、1元预算。审批记录见execution-approval.json；最终冻结哈希见approved-review-manifest.json。仅更新审批元数据及其哈希，所有实际请求正文和预期内容不变；启动前load_bundle已无授权缺项。实际结果以正式输出目录为准。


## 启动阻塞：凭据来源需要明确授权

用户已授权本批真实运行，但隔离目录没有.env，启动在读取配置阶段停止，未创建模型连接或正式输出目录。检查仅确认原项目C:/Users/hyf/Desktop/mini-agentic-rag-agent/.env存在，没有读取其中内容。

尝试跨项目复用该配置的命令在执行前被自动审批拒绝，原因是用户未明确授权跨项目凭据来源。未通过替代方式绕过该拒绝。当前请求数0、响应数0，本批模型调用费用尚未产生；不属于已发送后费用未知的情况。

批次、模型、15次及1元授权保留，冻结内容未变。只需就原项目.env这一明确来源取得授权后继续启动，不另加请求、不重新审批已接受规模。详细状态见startup-status.json。


## 实际执行结果：基础初筛未通过

用户明确授权原项目两个连接项后，本批15次均完成，未自动重试。CAL-05三次均错误判两项增量为是；CAL-02三次存在必要事实支持与一般登记的范围争议。其余9条核心一致。见[正式报告](../../../../../../evaluation/results/tool-contribution-calibration/calibration-20260912-01/calibration-report.md)。按API用量与对应时段/缓存费率计算约0.041元，未核验账单实扣；程序高峰未缓存保守计算0.128522元。未追加请求或生成策略候选。
